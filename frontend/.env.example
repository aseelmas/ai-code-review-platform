# Development defaults to http://127.0.0.1:8000 when this is blank.
# For production builds, set this to your backend's public HTTPS URL.
# This value is PUBLIC and embedded at build time. Never put secrets here.
VITE_API_URL=
